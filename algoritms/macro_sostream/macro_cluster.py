class MacroCluster:

    def __init__(self, centroid, number_micro_clusters=1, radius=0, micros=[]):
        self.centroid = centroid
        self.number_micro_clusters = number_micro_clusters
        self.radius = radius
        self.micros = micros

    pass
